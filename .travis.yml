language: python
python:
  - "3.6"
install:
  - "pip install pipenv"
  - "pipenv install --dev"
  - "pip install coveralls"
script:
  pipenv coverage run --source=hamlet manage.py test
after_success:
  coveralls
